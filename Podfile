# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Dron3r' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Dron3r

  # FYI: "Any pod that you specify in your Podfile (with source options such as :path)
  # will override the same pod being used down in the dependency graph

  #

  pod 'Swifter', :git => 'https://github.com/httpswift/swifter.git'
  pod 'Starscream'
  pod 'SwiftSocket'

  #

  target 'Dron3rTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Dron3rUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
